// Slot color hex values — used by both the host and player screens via inline
// styles so the colors are always guaranteed to match exactly.
export const SLOT_COLOR_HEX = {
  red:    '#FF4949',
  blue:   '#2D7DD2',
  yellow: '#E6A500',
  green:  '#2ECC71',
}
